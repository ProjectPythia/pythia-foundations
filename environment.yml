name: pythia-book-dev
channels:
  - conda-forge
dependencies:
  - python=3.8
  - jupyter-book
  - matplotlib
  - numpy
  - pre-commit
  - sqlalchemy<1.4
  - cartopy
  - xarray
  - pip
  - pip:
      # works for regular pip packages
      - pythia-datasets
